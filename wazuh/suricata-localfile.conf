<!-- Add this localfile block to /var/ossec/etc/ossec.conf inside the Wazuh manager
     if the manager volume does not already include Suricata ingestion. -->
<localfile>
  <log_format>json</log_format>
  <location>/var/log/suricata/eve.json</location>
</localfile>
